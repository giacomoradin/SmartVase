# SmartVase Data Structure and Topics (JSON Passthrough)

This document outlines the MQTT topics and corresponding Cloud Firestore paths used in the SmartVase system. In this architecture, Cloud Firestore acts as a direct intermediary, storing and forwarding JSON payloads between the ESP32 Hub, Flutter App, and Vision components via HiveMQ and Cloud Functions.

Firestore Path Convention:

For most topics, the Firestore path directly mirrors the MQTT topic structure.

If the MQTT topic path has an odd number of levels (e.g., .../telemetry), the final level is duplicated in the Firestore path to ensure an even number of levels (e.g., .../telemetry/telemetry), creating a specific document for that topic's data. This is necessary because Firestore paths must alternate collection/document.

Exception for Logs: Log events are stored differently. Each log entry is added as a new document with a Firestore auto-generated ID within the logs collection (smartvase/{device_id}/logs/{auto_id}).

## 1. Telemetry Data

Purpose: Periodic sensor readings and device status sent from the ESP32 Hub.

Direction: ESP32 Hub -> HiveMQ -> Cloud Function -> Firestore -> Flutter App

MQTT Topic: smartvase/{device_id}/telemetry

Firestore Path: smartvase/{device_id}/telemetry/telemetry (Document ID: telemetry)

JSON Payload Example:

```
{
  "timestamp_utc": 1678886400,
  "uptime_s": 12345,
  "battery_voltage": 4.1,
  "water_level_cm": 14.5,
  "soil_moisture": 550,
  "temperature_c": 22.5,
  "humidity_percent": 45.0,
  "lux": 800,
  "device_status": "NOMINAL"
}
```


## 2. Log Events

Purpose: Event logs generated by the ESP32 Hub (e.g., errors, warnings, info).

Direction: ESP32 Hub -> HiveMQ -> Cloud Function -> Firestore ( -> Flutter App, potentially for display or analysis)

MQTT Topic: smartvase/{device_id}/logs

Firestore Path: smartvase/{device_id}/logs/{auto_generated_id} (Collection: logs, Document ID: Firestore Auto-ID)

JSON Payload Example (Structure - specific fields depend on the event):

```
{
  "timestamp_utc": 1678886500,
  "level": "ERROR", // e.g., INFO, WARN, ERROR
  "event": "WATER_LEVEL_LOW",
  "detail": "Water level below 5cm",
  "source_device": "HUB_01"
  // ... other relevant fields may be included
}
```



## 3. Configuration Command

Purpose: Settings sent from the Flutter App to configure the ESP32 Hub's behavior (e.g., plant type, sensor thresholds).

Direction: Flutter App -> Firestore -> Cloud Function -> HiveMQ -> ESP32 Hub

MQTT Topic: smartvase/{device_id}/command/config

Firestore Path: smartvase/{device_id}/command/config

JSON Payload Example:

```
{
  "plant_api_id": 451,
  "min_lux": 300,
  "max_lux": 1500,
  "target_soil_moisture": 600
}
```



## 4. Image Ready Notification

Purpose: Notification sent by the ESP32 Hub after successfully uploading a new image to storage.

Direction: ESP32 Hub -> HiveMQ -> Cloud Function -> Firestore -> Vision Component

MQTT Topic: smartvase/{device_id}/vision/image

Firestore Path: smartvase/{device_id}/vision/image

JSON Payload Example:

```
{
  "timestamp_utc": 1678886400,
  "image_url": "...",
  "resolution": "144x144"
}
```



## 5. Vision Analysis Result

Purpose: Results of the image analysis performed by the Vision component.

Direction: Vision Component -> Firestore -> Cloud Function -> HiveMQ -> ESP32 Hub (and potentially read by Flutter App)

MQTT Topic: smartvase/{device_id}/vision/result

Firestore Path: smartvase/{device_id}/vision/result

JSON Payload Example:

```
{
  "timestamp_utc": 1678886405,
  "leaf_health": "healthy", // e.g., healthy, warning, critical
  "suggestion": "add nitrogen", // Optional action suggestion
  "alert_water": null, // or "more", "less"
  "alert_light": "less light", // or "more", null
  "color_dominant_hex": "#008000",
  "knn_confidence": 0.85 // Example metric from the vision model
}
```

